= form_for @user, :as => :user, :url => users_path do |f|
  %fieldset
    %legend New User
    = render :partial => 'form', :locals => {:user => @user, :form => f}
    %button.button.positive{:type => 'submit'}    
      Create
      = image_tag('buttons/tick.png')
= link_to 'Back', users_path
